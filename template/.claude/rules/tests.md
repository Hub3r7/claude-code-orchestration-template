---
globs: tests/**/*
---

# Test file rules

- Follow existing test patterns in the project.
- Every component must have at least a smoke test.
- Do not mock external dependencies unless explicitly discussed — prefer integration tests.
- Test names should describe the behavior being tested, not the implementation.
